# AGENTS.md — FlowWiki Codex/Gemini Agent Bootstrap

## 快速测试
```bash
python _scripts/bootstrap.py --source raw/enforcement-review --slug enforcement-review --skip-to 2
python _scripts/hermes_review.py --industry enforcement-review
```

详见 TESTING.md
