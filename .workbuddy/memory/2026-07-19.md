# 2026-07-19 FlowWiki 工作日志

## 掘金文章自动化执行（22:00 定时）
- 第2次执行：第1次执行产物（draft文件）未实际持久化，重新撰写 article-02
- article-02：ACE 反思循环防幻觉机制深度拆解，约 3000+ 字
- 五段结构：痛点（RAG 幻觉率数据）→ ACE 四 Agent 原理 → FlowWiki 实现细节（5 维度评分/原文指针铁律 7 项检查/Jaccard 去重）→ 实战演示 → 竞品对比
- 草稿路径：ops/publishing/series/drafts/article-02.md
- article-plan.yaml：article-02 status draft→ready_for_review，新增 draft_date/draft_file 字段
- 结合今日 git：ACE 原文指针铁律 (23a2678)、lint 增强、strict 模式、原文指针段强制检查
- 下一篇：article-03（A-MEM 卡片记忆系统）

## 每日全量测试（自动化任务）
- 执行 daily_test.py，修复 4 个脚本 bug 后全部通过
- Phase 1-3, 6 ✅；Phase 4 Hermes API 余额不足；Phase 5 Docker 未运行
- 修复内容：ops_log 动态导入、inter_density KeyError、未定义变量 p1_total/p3_pass/p3_total、wikilink basename 断链误报（441→0）
- 报告已保存 ops/monitoring/daily-test-2026-07-19.md + .json，同步至 junge-hermes/仪表盘/

## 宪法"全文入库"条款核查
- 用户问 SCHEMA.md（知识库宪法）是否要求全文入库
- 结论：宪法**没有**"全文入库"要求。通篇 225 行无此四字
- 宪法实际立场：raw/ 存原文（只读证据层）+ wiki/ 存精炼版（AI 编译层）+ 可追溯即可
- 关键条款：§1.1 CIC（Compile=编译非搬运）、§1.3 证据链完整（可追溯非全文）、§4.2 wiki 规范（无全文要求）
- 执法库 `raw/enforcement-review/工具资源/法条速查指针/法条指针总览.md` 印证："不搬运大段法条"
- 易混点：首页"旧库112篇权威全文已全量入仓"指 raw/ 自包含，非 wiki/ 全文入库。两者不矛盾

## ACE 质量要求梳理
- 来源：SCHEMA.md §4.3/§5.1/§6.2 + spec/design.md §2.L4 + §5 风险权衡
- 三阶段要求：Generator（溯源+frontmatter+confidence）→ Reflector（三必查：矛盾/幻觉/过时）→ Curator（四种处置：入wiki/标待核/触发conflict/退回）
- 整体护栏 7 条：3 轮上限、证据链、.memory/ace 记忆留存、回退机制、矛盾追踪、人工否决、成本控制（低频可关 Reflector）
- 矛盾自动覆盖条件：confidence=high + 新 source 权威
- **⚠️ Gap**：`_scripts/ingest_pipeline.py` 的 `run_ace_review()` 是 stub（永远返回 approved），ACE 三 agent 在代码层未落地，目前入仓实质裸奔

## 原文指针铁律实施（v2.1 升级，已推送到 git）
- 用户先要求"强制全文入库"，我提了 4 个反对（打破分层/冗余双写/检索降效/违反 §4.2），用户改选"指针+按需展开"方案
- **宪法层 SCHEMA.md**：§6 入库标准新增"原文指针铁律"（第 3 条），§8 操作纪律新增 2 条，§9 验收红线新增 2 项；版本升至 v2.1
- **spec/design.md**：新增决策 5（原文指针铁律）+ 决策 6（为什么不存全文）+ 三方案对比表
- **_scripts/ingest_pipeline.py**：`run_ace_review()` 从 stub 升级为真实实现，7 项强制检查：
  1. frontmatter 存在且含 sources
  2. ## 摘要 段非空
  3. ## 原文指针 段存在
  4. 全文路径字段
  5. 引用规则字段
  6. 路径指向真实 raw 文件（默认 raw_root = 项目 raw/）
  7. 全文搬运启发式检查（≥3 次"第X章"视为搬运）
  缺任一项返回 needs_revision
- **_templates/wiki_page.md.j2**：模板加 ## 摘要 + ## 原文指针 双段
- **新增 fulltext skill**（.agents + .claude 双格式部署）：按需加载 raw/ 全文，配套指针铁律
- **lint skill 升级**：新增宪法强制检查项（原文指针段/完整性/有效性/零全文搬运），违规 severity=high
- **冲突处理**：rebase 时 SCHEMA.md 和 spec/design.md 冲突——远程已把 SCHEMA 改为"执法库专属宪法"（结构 §1-8，主题是运维公约），保留远程结构，把铁律融入 §6/§8/§9
- **路径教训**：rebase 后 raw/enforcement-review/ 目录从中文（00_首页与导航/）重组为英文（articles/laws/regulations/standards/），旧测试路径过时，换新路径通过
- Git: commit 87e811d，推送成功到 origin/main

## lint.py 4 项扩展（基于 AI 使用反馈报告，已推送到 git）
- 用户提供了详细的 AI 使用反馈（化工危废暂存间查询），5 个"做得不好"问题映射为 6 项改进
- 用户选择执行 4 项：#1 旧字段检测、#2 时效性引用检查、#3 标准页限值表检查、#5 index 自动同步
- **#1 旧字段检测** (`lint.py check_legacy_fields`)：
  - `LEGACY_FIELD_MAP` 映射 8 个旧字段 → 新字段名 (category→type, priority→风险等级, weight→废弃 等)
  - 残留旧字段会让 AI 困惑"用哪个"，lint 报警提示迁移目标
- **#2 时效性引用检查** (`lint.py check_obsolete_references`)：
  - 新建 `storage/enforcement-review/法典取代映射.yaml`：10 部被生态环境法典废止的法律 + 别名
  - 扫描 `[[xxx]]` wikilink 是否引用已废止法律，提示改为 `[[生态环境法典]]` 对应编章
  - 2026.8.15 前过渡期（warning），之后 error
- **#3 标准页限值表检查** (`lint.py check_standard_limits_table`)：
  - 新建 `_templates/standard.md.j2`：标准页模板含 `## 核心限值表` 强制段
  - 检查 `type=standard` 页面是否含限值表且非空，解决"标准页最刚需的限值数值反而是缺的"
- **#5 index 自动同步** (`ingest_pipeline.py`)：
  - `compile_to_wiki()` 移除内联 index 生成（只覆盖 3 类），末尾调用 `reindex.main()` 全量重建（6 类）
  - 解决"加了页面但 wiki/index.md 不更新"的问题
- **测试**：3 项 lint 单元测试全部通过（旧字段 4 命中 / 时效性 1 命中 / 标准页 3 场景通过）
- **当前 wiki lint 状态**：147 文件 950 问题（主要是骨架页 sources 为空，新检查项在现有 wiki 中 0 命中 = 预防性检查）
- Git: commit b2193d3，推送成功到 origin/main

## AI 使用反馈 → 7 条建议逐条评估 + 4 项迭代（已推送 997a99f）
- 用户转发了 AI 对整个使用流程（初始化→修复→入库→查询）的 7 条迭代建议
- **核实后发现 3 条前提过时**：#4 图驱动索引已做完(b2193d3)、#5 entities 目录不存在、#1 lint 已有 blacklist
- **额外发现更紧急的 P0**：`status: 现行`(120页) + `status: inbox`(11页) 不在 VALID_STATUS 中

### 执行的 4 项迭代

**P0: status 字段合法值修复** (lint.py)
- VALID_STATUS 新增 "现行", "inbox", "deprecated", "已废止"
- 消除 131 个误报（950→819）

**P0: --strict 严格模式** (lint.py)
- LEGACY_FIELD_MAP 从 8 扩至 13 字段（合并 subcategory/source/generated/last_updated）
- 新增 FORBIDDEN_FIELDS 集合（10 个绝对禁止字段）
- --strict CLI flag：旧字段升为 `[禁止字段]` error 级别，非零退出码
- 同步修复 12 个 wiki 页：`source`→`sources`（单数→复数）

**P1: 引用依赖追踪** (graph.py)
- 新增 `check_timeliness()`：status=deprecated 页 → 反向遍历引用者
- `--format check-timeliness`：输出"受影响页面"清单 + 修复建议
- 单元测试：固废法(deprecated) → 危废处置手册(引用者) 正确检测
- 当前 wiki：0 个 deprecated 页，功能就绪待用

**P1: 知识缺口发现** (lint.py)
- 新增 `check_coverage()` + `--check-coverage`：raw→wiki 反向覆盖
- 三级匹配：路径精确 → 文件名 → 词级模糊（中文实词拆分）
- 当前结果：15 个高完整度 raw 文件，5 个被引用（覆盖 33.3%），10 个裸资源
- 裸资源清单：水污染防治法、土污法、海洋法、固废法、噪声法、放射性法、环评法等

### 未执行的 3 条
- #3 分层字段模型：已有 per-type 检查函数，分层模型反而更刚性
- #4 图驱动索引：已做完（b2193d3），唯一新点子是"状态摘要块"可后续加
- #5 entities 豁免：enforcement-review/entities/ 有 16 文件但不是 type=entity，优先级低

### 修改的文件
- `_scripts/lint.py` (+180 行): status 修复 + strict 模式 + coverage 检查
- `_scripts/graph.py` (+45 行): timeliness 依赖追踪 + yaml 导入
- `wiki/enforcement-review/` 12 文件: source→sources 修复
- Git: b2193d3..997a99f, 14 files changed, 271 insertions, 22 deletions

## W-Loop 建议评估 → playbook 模板 + coverage 增强（已推送 b4ca463）
- AI 提出"W-Loop 工作流迭代环"4 项修改方案
- 核实发现 3/4 是已有功能换了个名字：.memory/gaps/ 已存在(24个)、wiki/playbooks/ 已存在、type=playbook 已是合法值
- 还有命名冲突：wiki/skill/ 会和 .agents/skills/(30个) 混淆
- 采取两处小改动而非重做架构：
  1. _templates/wiki_page.md.j2：type=playbook 加 ## 验证清单 + ## 迭代记录（concept/law不受影响）
  2. lint.py --check-coverage：输出从裸文件列表变成"裸资源 → 建议 playbook 类型"（17个关键词映射）
- 不改目录结构、不重命名、不重复造轮子
- Git: 997a99f..b4ca463, 2 files, +51 lines

## 5行业测试驱动 → 路由完整性 + 限值表标准化（已推送 11ac71d）
- 用户完成 5 行业测试（化工/钢铁/食品/制药/养殖），发现 10 个标准路由断链
- AI 提出 4 项迭代，核实后执行 2 项 P0：
- **#1 --check-routing** (lint.py)：扫描 industry.yaml → 检查引用标准页是否存在
  - 兼容 3 种格式（standards 列表 / standard_criterion / industries[].standard）
  - 正则去分隔符模糊匹配（GB 31571 ↔ GB31571）
  - 当前 enforcement-review 无标准编号引用（通用设计，多行业配置就绪即用）
- **#2 限值表标准化** (_templates/standard.md.j2 + lint.py)：
  - 模板定死 5 列：污染物 | 限值 | 单位 | 适用条件 | 引用条款
  - lint 增强：解析 Markdown 表格 → 校验列名 → 检查数据行 → 报"待补充"占位符
  - 列名不一致时报具体列名和期望值
  - **不双写 YAML**——AI 和人力读同一张 Markdown 表，无漂移风险
- #3 案例关联和 #4 行业首页暂缓（需先攒内容）
- Git: b4ca463..11ac71d, 2 files, +180/-13
