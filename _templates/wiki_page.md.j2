---
type: {{ type | default("concept") }}
title: {{ title }}
created: {{ created | default("2026-07-19") }}
updated: {{ updated | default("2026-07-19") }}
confidence: {{ confidence | default("medium") }}
sources: [{% for s in sources %}"[[raw/{{ s }}]]"{% if not loop.last %}, {% endif %}{% endfor %}]
tags: [flow-wiki, {{ type | default("concept") }}{% for t in tags %}, {{ t }}{% endfor %}]
status: {{ status | default("draft") }}
---

# {{ title }}

## 摘要

{{ overview | default("一句话主旨 + 判断要点 + 触发条件") }}

## 核心内容

{{ content }}

{% if type == "playbook" %}
## 验证清单

{% for item in checklist | default(["- [ ] 每一步都有法律/标准支撑", "- [ ] 无模糊表述", "- [ ] 关键术语有定义"]) %}
{{ item }}
{% endfor %}

## 迭代记录

| 版本 | 日期 | 修改内容 |
|------|------|---------|
| v1.0 | {{ created | default("2026-07-19") }} | 初版 |
{% if iterations %}{% for iter in iterations %}| {{ iter.version }} | {{ iter.date }} | {{ iter.change }} |{% endfor %}{% endif %}

{% endif %}

## 原文指针

- 全文路径：`../raw/{{ raw_path | default("<subdir>/<file>.md") }}`
- 引用规则：逐字引用到条/款/项，引用后回链本页
- 加载方式：通过 `/fulltext` skill 按需 read

## 相关资料

{% for source in sources %}
- [{{ source }}]({{ source }})
{% endfor %}

## 关联知识

{% for related in related %}
- [{{ related.name }}]({{ related.path }})
{% endfor %}
