---
type: standard
title: {{ title }}
created: {{ created | default("2026-07-19") }}
updated: {{ updated | default("2026-07-19") }}
confidence: {{ confidence | default("medium") }}
sources: [{% for s in sources %}"[[raw/{{ s }}]]"{% if not loop.last %}, {% endif %}{% endfor %}]
tags: [flow-wiki, standard{% for t in tags %}, {{ t }}{% endfor %}]
status: {{ status | default("draft") }}
standard_number: {{ standard_number | default("GB/T XXXXX-XXXX") }}
issue_date: {{ issue_date | default("YYYY-MM-DD") }}
implement_date: {{ implement_date | default("YYYY-MM-DD") }}
---

# {{ title }}

## 摘要

{{ overview | default("标准编号、发布日期、实施日期、适用范围、核心要求一句话概括") }}

## 核心限值表

> ⚠️ lint 强制项。列名固定（「污染物」「限值」「单位」「适用条件」「引用条款」），
> 不得增减列。AI 和人力读同一张表，无 YAML 双写漂移风险。

| 污染物 | 限值 | 单位 | 适用条件 | 引用条款 |
|--------|------|------|---------|---------|
| {{ item.name | default("VOCs") }} | {{ item.value | default("100") }} | {{ item.unit | default("mg/m³") }} | {{ item.condition | default("新建企业") }} | {{ item.article | default("表3 第1项") }} |
| {{ item2.name | default("VOCs") }} | {{ item2.value | default("120") }} | {{ item2.unit | default("mg/m³") }} | {{ item2.condition | default("现有企业") }} | {{ item2.article | default("表3 第1项") }} |
| （继续添加...） | | | | |

## 适用范围

{{ scope | default("本标准适用于……") }}

## 原文指针

- 全文路径：`../raw/{{ raw_path | default("enforcement-review/standards/<file>.md") }}`
- 引用规则：逐字引用到条/款/项，引用后回链本页
- 加载方式：通过 `/fulltext` skill 按需 read

## 相关资料

{% for source in sources %}
- [{{ source }}]({{ source }})
{% endfor %}

## 关联知识

{% for related in related %}
- [{{ related.name }}]({{ related.path }})
{% endfor %}
